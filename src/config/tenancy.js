/**
 * Shared across all organizations (exercise content catalogue).
 * Everything else is tenant-scoped via `orgId`.
 */
export const SHARED_COLLECTIONS = ["exercises", "topics", "levels", "vocabdecks"]
